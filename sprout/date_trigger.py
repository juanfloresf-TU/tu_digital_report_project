from datetime import  date
today = date.today()

# Check if the day of the month is 1
if today.day == 1:
    print("It's the first day of the month!" + str(today))

    ## append to dataset 
    ## ejecutar mes anterior y guardar en dataset _dwn_m
    
    
else:
    print("It's not the first day of the month." + str(today))
   